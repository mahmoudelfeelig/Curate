services:
  agentcore:
    build:
      context: ../../services/curator
      dockerfile: Dockerfile.agentcore
    environment:
    ports:
      - "8080:8080"
    read_only: true
    tmpfs:
      - /tmp:size=64m
    security_opt:
      - no-new-privileges:true
