{
  "name": "feed-passport-agentcore-infra",
  "version": "0.1.0",
  "private": true,
  "description": "Plan-first AWS CDK definition for the Feed Passport proposal-only AgentCore runtime",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js && python test/validate_package_test.py",
    "synth": "npm run build && node dist/scripts/synth-local.js",
    "validate:template": "npm run synth && node dist/scripts/validate-template.js cdk.out/FeedPassportAgentCore.template.json"
  },
  "dependencies": {
    "aws-cdk-lib": "2.267.0",
    "constructs": "10.8.1"
  },
  "devDependencies": {
    "@types/node": "26.4.0",
    "aws-cdk": "2.1139.0",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
